# Feature and documentation audit — 4.0

Feature level: **40**. The `.1` suffix changes portability only.

The executable contract is checked by `tests/feature-matrix-test.sh`; the EIGIIB
documentation layers and help snapshot are checked by
`tests/eigiib-documentation-test.sh`. Runtime behavior remains covered by the
local, network, and security suites listed in `TESTING.md`.
